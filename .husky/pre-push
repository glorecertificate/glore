#!/usr/bin/env sh

tsc --noEmit
eslint . 
prettier --check .
# shellcheck disable=SC2046
shellcheck $(find .husky -maxdepth 1 -type f)
commitlint --from origin/main --to HEAD --verbose

pnpm build
pnpm check
