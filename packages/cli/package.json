{
  "name": "@repo/cli",
  "bin": {
    "glore": "./cli.sh"
  },
  "scripts": {
    "lint": "shellcheck *.sh **/*.sh",
    "check": "pnpm lint"
  },
  "devDependencies": {
    "shellcheck": "catalog:"
  }
}
